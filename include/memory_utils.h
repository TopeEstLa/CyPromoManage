#ifndef MEMORY_UTILS_H
#define MEMORY_UTILS_H

/**
 * Swap the order of elements in a block of memory.
 * @param data
 * @param size
 * @param count
 * @return
 */
void* swap_order(void* data, int size, int count);

#endif //MEMORY_UTILS_H